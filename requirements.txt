from flask import Flask

app = Flask (__name__)
#only include while testing
app.config["DEBUG"] = True

@app.route("/")
def home()
	return "INSERT WHOLE PAGE HERE"

if __name__=="__main__":
	app.run(host="0.0.0.0")